interface:
  display_name: "Project Manager"
  short_description: "Triage intake and report queue status"
  default_prompt: "Use $project-manager to triage this agent-tracker project, report queue status, and propose or promote follow-up tasks without taking a worker lease."
